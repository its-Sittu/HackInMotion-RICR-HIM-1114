services:
  - type: web
    name: pulsemed-backend
    env: node
    plan: free
    region: singapore
    buildCommand: cd backend && npm install
    startCommand: node backend/src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
